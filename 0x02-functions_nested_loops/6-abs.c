#include "main.h"

/**
 * _abs - checks digit
 * @int: the intger to check
 *
 *
 * Return: positive value of int.
*/

int _abs(int)
{
	int n;

if (n > 0)
{
printf("%d", n);

}
else if (n == 0)
{
printf("%d", n);
}
else
{
n = n * -1;
printf("%d", n);
}
