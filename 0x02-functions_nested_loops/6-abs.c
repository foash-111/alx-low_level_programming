#include "main.h"

/**
 * _abs - checks digit
 * @int: the intger to check
 *
 *
 * Return: positive value of int.
*/

int _abs(int)
{
	int n;

if (n > 0)
{
return (n);

}
else if (n == 0)
{
return (n);
}
else
{
return (-n);
}
