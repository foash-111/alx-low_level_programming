#include "main.h"

/**
 * _abs - checks digit
 * @n: the intger to check
 *
 *
 * Return: positive value of int n.
*/

int _abs(int n)
{

if (n > 0)
{
return (n);

}
else if (n == 0)
{
return (n);
}
else
{
return (-n);
}
